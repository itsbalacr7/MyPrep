# Write your MySQL query statement below
with FIrst_login as 
(SELECT 
player_id,
device_id,
event_date,
games_played,
MIN(event_date) as first_login
FROM Activity)
SELECT 
player_id
first_login
FROM First_login as F 
group by player_id;
